{
    kafka-producer {
        address = "localhost:9092"
        topic = "events"
        client = "client"
    }

    postgres {
        //should be loaded from environment
        address = "jdbc:postgresql://localhost:5432/chess"
        user = "root"
        password = "notlongenoughpassword"
    }

    http-server{
    port = 6969
    host = "localhost"
    }
}